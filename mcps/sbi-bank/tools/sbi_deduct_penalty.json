{
  "name": "sbi_deduct_penalty",
  "description": "Deduct a penalty from the virtual SBI bank account for distraction/non-compliance. DEMO ONLY - No real money involved. Penalty amounts: Strike 1 = ₹50, Strike 2 = ₹100, Strike 3 = ₹200.",
  "arguments": {
    "type": "object",
    "properties": {
      "strike_count": {
        "type": "integer",
        "description": "Current strike count (1-3). Determines penalty amount.",
        "minimum": 1,
        "maximum": 3,
        "default": 1
      },
      "reason": {
        "type": "string",
        "description": "Reason for the penalty deduction",
        "default": "Distraction detected"
      }
    },
    "required": []
  },
  "endpoint": "http://localhost:8000/api/sbi/penalty",
  "method": "POST"
}
